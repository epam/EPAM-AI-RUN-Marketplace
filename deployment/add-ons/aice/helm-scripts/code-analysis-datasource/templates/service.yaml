apiVersion: v1
kind: Service
metadata:
  name: {{ include "code-analysis-datasource.fullname" . }}
  {{- with .Values.service.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
  labels:
    {{- include "code-analysis-datasource.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "code-analysis-datasource.selectorLabels" . | nindent 4 }}
