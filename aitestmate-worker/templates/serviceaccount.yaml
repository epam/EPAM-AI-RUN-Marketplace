---

apiVersion: v1
kind: ServiceAccount
metadata:
  name: aitestmate-worker-sa
  labels:
    # {{ include "aitestmate-worker.labels" . | nindent 4 }}
  # {{ with .Values.serviceAccount.annotations }}
  annotations:
    # {{ toYaml . | nindent 4 }}
  # {{ end }}
