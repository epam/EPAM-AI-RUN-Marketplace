apiVersion: v1
kind: Service
metadata:
  name: {{ include "aitestmate-nginx.fullname" . }}
  labels:
    {{- include "aitestmate-nginx.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "aitestmate-nginx.selectorLabels" . | nindent 4 }}
