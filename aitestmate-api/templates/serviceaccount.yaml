---

apiVersion: v1
kind: ServiceAccount
metadata:
  name: aitestmate-api-sa
  labels:
    # {{ include "aitestmate-api.labels" . | nindent 4 }}
  # {{ with .Values.serviceAccount.annotations }}
  annotations:
    # {{ toYaml . | nindent 4 }}
  # {{ end }}
