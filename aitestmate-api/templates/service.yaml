apiVersion: v1
kind: Service
metadata:
  name: aitestmate-api
  labels:
    {{- include "aitestmate-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "aitestmate-api.selectorLabels" . | nindent 4 }}
